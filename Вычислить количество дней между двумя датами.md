```scala
import org.apache.spark.sql.functions._

customers.withColumn(
    "profileAge",
    datediff(  // <= NB!
        lit(targetDate),  // или дата col("date")
        col("create_date")
    )
).show(100, 300, true)
```

